# OpenSpec / OPSX Artifacts: Agent-to-Agent Escrow Infrastructure

This folder is structured as an **OpenSpec change** (OPSX workflow style):

- `proposal.md` — why this change matters and what we will build
- `design.md` — architecture and key decisions
- `specs/` — requirements and scenarios
- `tasks.md` — implementation checklist

To use in a repo initialized with OpenSpec, place this folder under:

`openspec/changes/agent-to-agent-escrow-infrastructure/`
